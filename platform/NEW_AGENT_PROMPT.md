Paste this when starting a **new Cloud Agent** on `CoachBilliott/sports-website`:

---

You are working in the **sports-website** repo.

**Scope only:** the standalone app in `platform/` (port 3001).

**Do not change** the football Team OS under `src/` (AppShell, depth, schedule, etc.) unless I explicitly ask.

Read and follow:
- `platform/AGENTS.md`
- `platform/PLAN.md`

Goal: polish and expand the Platform UI for an AD demo — district/legal readiness, multi-sport “Add team”, Fan page, Parent portal, audit, export/delete.

Run with `cd platform && npm install && npm run dev` → http://localhost:3001

Commit on a `cursor/...-64b7` branch and open/update a PR against `master`.

---
